services:
  - type: web
    name: spamly-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend_bridge:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
